Hello <%= @user.name %>,

A new coaching session has been scheduled. Here are the details:

Title: <%= @session.title %>
Description: <%= @session.description %>
Date: <%= @session.scheduled_at.strftime('%B %d, %Y') %>
Time: <%= @session.start_time.strftime('%I:%M %p') %> - <%= @session.end_time.strftime('%I:%M %p') %>

<% if @role == 'Coach' %>
Clients: <%= @session.clients.map(&:name).join(', ') %>
<% else %>
Coaches: <%= @session.coaches.map(&:name).join(', ') %>
<% end %>

You can view the full session details by logging into your account.

Best regards,
Mini Coaching Team
